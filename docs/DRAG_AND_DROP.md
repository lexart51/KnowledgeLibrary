# Drag And Drop

When enabled in settings, vault files can be dragged onto the Knowledge Library view.

Dropping a valid vault file opens the Add Resource wizard with that file selected and its type detected. Nothing is created until the user confirms the wizard.

Files outside the active vault, excluded folders, configured resource-note folders, hidden folders, and disallowed extensions are rejected with a notice.

Drag and drop can be disabled from plugin settings.
